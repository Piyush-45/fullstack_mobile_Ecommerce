## STEPS TO BUILD FROM TS
1. make all local imports to have .js in end
2. use --env.file=.env (just give the adres of env file before running node command)
   -- node --env.file=.env dist/src/index.js
   
